namespace Shared.DTOs.ProductDTOs
{
    // Nested DTOs (can be in their own files if they get complex)
    public class FileDTO { public int Id { get; set; } public string Name { get; set; } = string.Empty; public string FileUrl { get; set; } = string.Empty; }
}
